// fn setup_redis_connections() -> RedisConnections {
//     let redis_connections = RedisConnections::new().unwrap();
//     flush(redis_connections)
// }

// fn flush(redis_connections: &RedisConnections) -> RedisConnections {
//     redis_connections.flush_database().unwrap();
//     redis_connections
// }
